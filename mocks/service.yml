namespace: microserviceRoot

public:
  - web

services:
  web:
    image: node:4
    ports:
      - "3000:3000"
    volumes:
      - ..:/microserviceRoot
    working_dir: /microserviceRoot
    command: [npm, run, start-mocks]

setup:
  - cd .. && npm install
